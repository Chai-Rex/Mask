using UnityEngine;
using UtilitySingletons;

public class DialogManager : PersistentSingleton<DialogManager>
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    
}
